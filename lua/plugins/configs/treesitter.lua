local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "c_sharp", "golang" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
